;;; config-apps.el --- Applications and larger packages -*- lexical-binding: t; -*-

(use-package magit
  :custom
  (package-install-upgrade-built-in t))

(use-package ein)

(provide 'config-apps)
;;; config-apps.el ends here
